services:
  dbt-core:
    build:
      context: .
    stdin_open: true
    tty: true
    ports:
      - "8085:8080"
    env_file : ./config/.env
    volumes:
      - type: bind
        source: .
        target: /app
    